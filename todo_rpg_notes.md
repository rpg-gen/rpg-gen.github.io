# assorted

When looking at lore or player cards, the mentions of notes should be in descencing order by session and created date

For inventory lists on players or unassigned, show the number right-aligned in the space, with title left-aligned. Also let's band the list so it's easy to look across and see the right number/quantity for what we are looking for.

For the lore quick filters, the filters seem cutt off by each other until selected. IDeally the plus button would always be visible as well, and when nothing selected they should not be overlapping and cutting each other off. Validate we've gotten this right by checking how it looks in playwright.

When typing notes, we need to have a placeholder that indicates they can use [[ or / to start a lookup.

Also, the rules floating bottom button should only show up when on the rpg notes part of the app.

Also, we need to rename the feedback so it's for the whole rpg site, not just notes.

Also for the party page, can we show the counts/renown/items/followers as a grid, with title above and quantity below, so it's the same for all of them, allways showing a follower count even if they dont' have any, etc.

Also, let's have an x visible to exit the rules right after opening it, so you can uickly close without having to hit the hamburger menu. Maybe top-right of the rules page?

Also, for Lore items assigned to players, show those in the player list below the 4-grid of int values 9 (renown, wealth, etc)

# Quests

Add todos for quests with 3/5 etc tracking of  total completed items