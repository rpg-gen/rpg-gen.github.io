import { themeStyles } from "../../configs/ttrpg_theme"

// Re-export from theme — keeps existing import names working
export const cardStyle = themeStyles.card
export const primaryButtonStyle = themeStyles.primaryButton
export const primaryButtonSmallStyle = themeStyles.primaryButtonSmall
export const textareaStyle = themeStyles.textarea
export const tabStyle = themeStyles.tabStyle
