type type_scale_context = {
    hexagon_edge_pixels: number,
    num_hexes_tall: number,
    num_hexes_wide: number,
    set_scale_context: React.Dispatch<React.SetStateAction<type_scale_context>>,
}

export default type_scale_context