type ScaleContext = {
    hexagon_edge_pixels: number,
    num_hexes_tall: number,
    num_hexes_wide: number,
    set_scale_context: React.Dispatch<React.SetStateAction<ScaleContext>>,
}

export default ScaleContext