// Note: This more-complex structure for definitions is not currently being used
// was changeded in favor of just using a list of hexagon definitions

// import type_hexagon_definition from "./type_hexagon_definition"

// type type_hexagon_definitions = {[row_number: string]: {[column_number: string]: type_hexagon_definition}}

// export default type_hexagon_definitions